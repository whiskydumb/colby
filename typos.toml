[files]
# @note: `target/` is not here. It was, while this checkout was not a git
# repository and typos had no .gitignore to read; it has one now and honors it.
extend-exclude = ["Cargo.lock", "CHANGELOG.md"]

[default]
locale = "en-us"

[default.extend-words]
